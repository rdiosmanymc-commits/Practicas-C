#include <stdio.h>

int main (){
    int v;
    int x;

    // Segun la tabla 1.11, primero v toma un valor, y luego x toma el resultado final
    x = (v = 5, v * 4); 

    printf("El valor final de v es: %d\n", v);
    printf("El valor final de x es: %d\n", x);
    printf("yo creo que se imprimira:5 y 20 ");














    return 0;
}