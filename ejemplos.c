#include <stdio.h>
#include <stdbool.h>
 
int main() {
    int elpepe;
    scanf ("%d", &elpepe);
     bool resultado = (elpepe >= 1); // La operación da verdadero (1)
    
     printf("%d\n", resultado); // Imprime el valor como entero
    
    


    return 0;
}