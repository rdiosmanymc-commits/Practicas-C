#include <stdio.h>

int main() {
    int a, b, c;
    a = 10;
    b = 3;
    c = 4;
    printf("el resultado de a + b * c es: %d\n", a + b * c);
    printf("el resultado de (a + b) * c es: %d\n", (a + b) * c);
    printf("el resultado de a/b es: %d\n", a / b);
    
    return 0;
}